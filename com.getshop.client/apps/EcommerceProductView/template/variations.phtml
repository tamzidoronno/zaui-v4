<?
/* @var $this \ns_4404dc7d_e68a_4fd5_bd98_39813974a606\EcommerceProductView */
$product = $this->getProduct();
$extras = $product->extras;
?>
<div class='optiontemplate optionrow'>
    <div class='gs_shop_small_icon deleteoptionrow'><i class='fa fa-trash'></i></div>
    <div class='col col1'>
        <input class='gsniceinput1' name='name'/>
    </div>
    <div class='col col1'>
        <input class='gsniceinput1' name='extraPriceDouble'/>
    </div>
</div>

<?
if (is_array($extras) && count($extras)) {
    
    foreach ($extras as $extra) {
        ?>
        <div class='optionbox' optionid='<? echo $extra->id; ?>'>
            <h2><? echo $extra->name; ?> ( <? echo $extra->type; ?> )</h2>
            <div class='optionrows'>
                
            </div>
            <div class="buttons">
                <div style='background-color:red;' gs_confirm="Are you sure you want to delete the whole option?" gsclick="deleteExtraOption" class="shop_button" optionid="<? echo $extra->id; ?>"><? echo $this->__f("Delete option"); ?></div>
                <div class="shop_button addextraoption" optionid="<? echo $extra->id; ?>"><? echo $this->__f("Add option"); ?></div>
            </div>
        </div>
        <script>
            var data = <? echo json_encode($extra); ?>;
            app.EcommerceProductView.populateExtraOptions(data);
        </script>
    
        <?
    }
}
?>
        
<hr/>
<div gstype="form" method="addTopLevel">
    <input class="gsniceinput1" gsname="name" placeholder="<? echo $this->__("Name"); ?>">  
    <select class='gsniceselect1' gsname="type">
        <option value='checkbox'><? echo $this->__f("Checkbox"); ?></option>
        <option value='group'><? echo $this->__f("Group"); ?></option>
    </select>
    <div class="shop_button" gstype="submit"><? echo $this->__f("Add top level"); ?></div>    
</div>

