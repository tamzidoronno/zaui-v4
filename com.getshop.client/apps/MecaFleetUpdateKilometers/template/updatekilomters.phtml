<?
/* @var $this ns_3bbe1c5d_2f02_40dd_a16a_55aa46953300\MecaFleetUpdateKilometers */
$cars = $this->getApi()->getMecaManager()->getCarsForMecaFleet($this->getPage()->getId());
$fleet = $this->getApi()->getMecaManager()->getFleetPageId($this->getPage()->getId());

echo "<div class='fleettitle'>".$fleet->name."</div>";
?>

<div gstype="form" method="update">
    <div class="carline">
        <div class="col col1">Bilinfo</div>
        <div class="col col2">Ny kilomterstand</div>
    </div>
            
    <?
    foreach ($cars as $car) {
        ?>
        <div class="carline">
            <div class="col col1">
                <? 
                echo $car->licensePlate; 
                
                echo "<div>";
                    if ($car->kilometers) {
                        echo " Nåværende kilomterstand: ".$car->kilometers;
                    }
                    if ($car->requestKilomters && $car->requestKilomters->lastReceivedKilomters) {
                        echo ", Sist oppdatert: ".$car->requestKilomters->lastReceivedKilomters;
                    }
                echo "</div>";
                ?>
                
            </div>
            <div class="col col2"><input class="gsniceinput1" type="number" gsname="car_<? echo $car->id; ?>" placeholder="<? if ($car->kilometers) { echo $car->kilometers; } ?>"></div>
        </div>
        <?
    }
    
    ?>
    
    <div class="buttonarea">
        <?
        if ($_POST['event'] === "update") {
            ?>
            <div style="color: green; padding-top: 20px; padding-bottom: 20px;">
                <i class="fa fa-check"></i> Lagring fullført!
            </div>
            
            <?  
        }
        ?>
        <div class="shop_button" gstype="submit"><i class="fa fa-save"></i> Lagre</div>
    </div>
    
</div>