<?php
/* @var $this ns_ed7efba0_de37_4cd8_915b_cc7be10b8b8b\PmsBookingProductList */

$rooms = $this->getApi()->getPmsManager()->getAllRoomTypes($this->getSelectedName(), 0, 10);

$booking = $this->getCurrentBooking();

?>
<div class='continue_button_outer'>
    <div class='continue_button'><? echo $this->getText("next_button"); ?></div>
</div>
<?php

foreach($rooms as $room) {
    /* @var $room core_pmsmanager_Room */
    $selected = "";
    if(in_array($room->type->id, $booking->products)) {
        $selected = "selected";
    }
    ?>
    <div class='productentry <? echo $selected; ?>'>
    <span class='name'><? echo $room->type->name; ?></span>
    <span class='price'><? echo $room->price; ?>,-</span>
    <span class='select_button'><? echo $this->getText("select_button"); ?></span>
    </div>
    <?php
}
?>
<div class='continue_button_outer'>
    <div class='continue_button'><? echo $this->getText("next_button"); ?></div>
</div>
