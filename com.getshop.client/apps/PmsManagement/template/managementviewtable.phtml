<?php
/* @var $this ns_7e828cd0_8b44_4125_ae4f_f61983b01e0a\PmsManagement */
$filter = $this->getSelectedFilter();
$bookings = $this->getApi()->getPmsManager()->getAllBookings($this->getSelectedName(), $filter);
$types = $this->getApi()->getBookingEngine()->getBookingItemTypes($this->getSelectedName());
$items = $this->getApi()->getBookingEngine()->getBookingItems($this->getSelectedName());
$prices = $this->getApi()->getPmsManager()->getPrices($this->getSelectedName(), $this->convertToJavaDate(time()), $this->convertToJavaDate(time()));

$hasPrices = false;
if(($prices->defaultPriceType == 1 && !$prices->dailyPrices) || $prices->defaultPriceType != 1) {
    $hasPrices = true;
}
foreach($items as $item) {
    $items[$item->id] = $item;
}
$types2 = array();
foreach($types as $type) {
    $types2[$type->id] = $type;
}
$types = $types2;

$rows = 0;
$headers = array();
$headers['regdate'] = "Reg";
$headers['periode'] = "Periode";
$headers['visitor'] = "Visitor";
$headers['room'] = "Room";
$headers['price'] = "Price";
if(!isset($filter->sorting)) {
    $filter->sorting = "regdate";
}

if(!isset($filter->typeFilter)) {
    $filter->typeFilter = array();
}

foreach($types as $id => $type) {
    echo "<span class='typefilter'>";
    $checked = "";
    if(in_array($id, $filter->typeFilter)) {
        $checked = "CHECKED";
    }
    echo "<input type='checkbox' gstype='clicksubmitforce' method='addTypeFilter' gsname='id' gsvalue='$id' $checked>";
    echo $type->name . "</span>";
}
$checked = "";
if($this->isGroupedByBooking()) {
    $checked = "CHECKED";
}
echo "<input type='checkbox' gstype='clicksubmitforce' method='groupByBooking' gsname='id' gsvalue='' $checked> Group by booking";
echo "<br>";
echo "<br>";

echo "<table width='100%' cellspacing='0' cellpadding='0'>";
echo "<tr>";
echo "<th></th>";
foreach($headers as $header => $value) {
    if($header == "price" && !$hasPrices) {
        continue;
    }
    echo "<th gstype='clicksubmit' gsvalue='$header' method='sortTable' gsname='column' style='cursor:pointer;'>";
    if($filter->sorting == $header) {
        echo "<i class='fa fa-arrow-down'></i>";
    } else if($filter->sorting == $header . "_desc") {
        echo "<i class='fa fa-arrow-up'></i>";
    }
    echo $value;
    echo "</th>";
}
echo "<th align='center'><center>State</center></th>";
echo "</tr>";
if(!$bookings) {
    $bookings = array();
}
foreach($bookings as $booking) {
    foreach($booking->rooms as $room) {
        $rows++;
        if($types[$room->bookingItemTypeId]->addon > 0) {
            continue;
        }
        
        echo "<tr>";
        echo "<td style='width:70px;'>";
        echo "<i class='fa fa-info moreinformationaboutbooking actions' roomid='".$room->pmsBookingRoomId."' bookingid='".$booking->id."'></i> ";
        echo "</td>";
        echo "<td style='width: 100px;'>";
        echo date("d.m.Y H:i", strtotime($booking->rowCreatedDate));
        echo "</td>";

        echo "<td style='width:170px;'>";
        echo date("d.m.Y H:i", strtotime($room->date->start));
        if($room->date->end) {
            //Dont show end date if this is a atleast 75 years booking
            if(((strtotime($room->date->end) - strtotime($room->date->start)) /86400) < 20000) {
                if(date("dmy", strtotime($room->date->end)) !=  date("dmy", strtotime($room->date->start))) {
                    echo "<br>";
                    echo date("d.m.Y H:i", strtotime($room->date->end));
                } else {
                    echo " - " . date("H:i", strtotime($room->date->end));
                }
            }
        }
        echo "</td>";
        echo "<td>";
        foreach($booking->rooms as $room2) {
            $addons = array();
            if($types[$room2->bookingItemTypeId]->addon > 0) {
                $addons[] = $types[$room2->bookingItemTypeId]->name;
            }
            if($addons) {
                echo "<i class='fa fa-plus-circle' title='".  join(",", $addons)."'></i> ";
            }
        }
        foreach($room->guests as $guest) {
            echo $guest->name . " - " . $guest->email . " - " . $guest->phone . "<br>";
        }
        $user = $this->getApi()->getUserManager()->getUserById($booking->userId);
        echo "<div style='padding-top: 5px;'>";
        if($user) {
            echo "<span class='secondary_text'>By " . $user->fullName . "</span>";
        }
        echo "</div>";
        
        if($this->isGroupedByBooking() && sizeof($booking->rooms) > 1) {
            echo "+" . (sizeof($booking->rooms)-1) . " additional room(s)";
        }

        
        echo "</td>";
        echo "<td>";
        if($room->booking->bookingItemId) {
            echo $items[$room->booking->bookingItemId]->bookingItemName;
        }
        echo "<br><span class='secondary_text'>" . $types[$room->bookingItemTypeId]->name . "</span>";

        echo "</td>";
        if($hasPrices) {
            echo "<td>";
            echo $room->price;
            echo "</td>";
        }

        echo "<td align='center'>";
        if($booking->unConfirmed) {
            echo "Rejected";
        } else if($booking->isDeleted) {
            echo "Deleted";
        } else if($booking->confirmed) {
            echo "Confirmed";
        } else if(!$booking->confirmed) {
            echo "Unconfirmed";
        }
        echo "</td>";
        echo "</tr>";
        
        if($this->isGroupedByBooking()) {
            break;
        }
    }
} 
echo "</table>";
echo "<br>";
echo "$rows rows in total.";
?>