Enter email/name of the user: <input type='textfield' id='searchfield'>
<div class='result'>
    <?
    $users = $this->getSearchUsers();
    if (count($users)) {
        foreach ($users as $user) {
            $name = $user->fullName;
            $email = $user->emailAddress;
            $id = $user->id;
            $magicPen = "<a href='/impersonate.php?userId=$id'><i class='fa fa-magic'></i></a>";
            echo "<div userId='$id' class='userentrysearch'> $magicPen $name - $email</div>";
        }
    }
    ?>
</div>
